numpy
scipy #Need it for sklearn
pandas
matplotlib
seaborn
pip
ipykernel
notebook
jupyter-client
jupyter-core
mlflow>=3.0.0 # (MLflow Client)
# The image we are using (ghcr.io/mlflow/mlflow:v3.0.0)(MLflow tracking server)
# is version 3.0.0. Keeping them identical eliminates "weird" bugs. Otherwise, 
# the server will return a 404 error because it doesn't recognize the endpoint. 
scikit-learn
xgboost
pydantic
flask
flask_cors
gunicorn
prometheus-client # In order to Python code be able to talk to Prometheus.